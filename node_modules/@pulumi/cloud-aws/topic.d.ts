import * as aws from "@pulumi/aws";
import * as cloud from "@pulumi/cloud";
import * as pulumi from "@pulumi/pulumi";
export declare class Topic<T> extends pulumi.ComponentResource implements cloud.Topic<T> {
    private readonly name;
    readonly topic: aws.sns.Topic;
    readonly subscriptions: aws.sns.TopicSubscription[];
    readonly publish: (item: T) => Promise<void>;
    constructor(name: string, opts?: pulumi.ResourceOptions);
    subscribe(name: string, handler: (item: T) => Promise<void>): void;
}
